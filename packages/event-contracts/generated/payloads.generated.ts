/* AUTO-GENERATED by scripts/generate-types.mjs — DO NOT EDIT.
 * One interface per event payload, generated from schemas/*.json.
 * Regenerate: npm run generate:ts
 */


/**
 * Envelope wrapping every Healuxa domain event published to NATS JetStream. The typed `payload` is defined by the event-specific schema referenced in `schema_ref`.
 */

export interface EventEnvelope {
  /**
   * Globally unique event id. Consumers MUST dedupe/idempotency-key on this.
   */
  event_id: string;
  /**
   * Fully-qualified event type, `<context>.<event>`.
   */
  type: string;
  /**
   * Major schema version of this event type.
   */
  version: number;
  /**
   * Canonical $id of the payload schema for this exact version.
   */
  schema_ref: string;
  /**
   * When the domain fact occurred (not when published).
   */
  occurred_at: string;
  /**
   * Tenant scope; present on every event envelope and most payloads.
   */
  tenant_id: string;
  /**
   * Service that owns and emitted this event. MUST equal the registered owner.
   */
  producer: string;
  /**
   * OpenTelemetry trace id for correlation across services.
   */
  trace_id?: string;
  /**
   * Ties a logical chain of events to a single business operation.
   */
  correlation_id?: string;
  /**
   * event_id of the event that directly caused this one.
   */
  causation_id?: string;
  /**
   * Who/what triggered the underlying action.
   */
  actor?: {
    type: "user" | "provider" | "service" | "system";
    id?: string;
  };
  /**
   * Typed event body; shape defined by the event-specific schema.
   */
  payload: {};
}

/**
 * payload of appointment.requested (v1)
 */

export interface AppointmentRequested {
  /**
   * ULID or UUID identifier.
   */
  appointment_id: string;
  /**
   * ULID or UUID identifier.
   */
  user_id: string;
  /**
   * ULID or UUID identifier.
   */
  provider_id: string;
  /**
   * ULID or UUID identifier.
   */
  service_id: string;
  /**
   * ULID or UUID identifier.
   */
  milestone_id?: string;
  /**
   * RFC 3339 / ISO 8601 timestamp in UTC.
   */
  scheduled_at: string;
  /**
   * RFC 3339 / ISO 8601 timestamp in UTC.
   */
  requested_at: string;
}

/**
 * payload of appointment.created (v1)
 */

export interface AppointmentCreated {
  /**
   * ULID or UUID identifier.
   */
  appointment_id: string;
  /**
   * ULID or UUID identifier.
   */
  user_id: string;
  /**
   * ULID or UUID identifier.
   */
  provider_id: string;
  /**
   * ULID or UUID identifier.
   */
  service_id: string;
  /**
   * ULID or UUID identifier.
   */
  milestone_id?: string;
  /**
   * RFC 3339 / ISO 8601 timestamp in UTC.
   */
  scheduled_at: string;
  channel?: "app" | "web" | "whatsapp" | "email" | "sms" | "push" | "in_app" | "system";
}

/**
 * payload of appointment.accepted (v1)
 */

export interface AppointmentAccepted {
  /**
   * ULID or UUID identifier.
   */
  appointment_id: string;
  /**
   * ULID or UUID identifier.
   */
  user_id: string;
  /**
   * ULID or UUID identifier.
   */
  provider_id: string;
  /**
   * ULID or UUID identifier.
   */
  service_id: string;
  /**
   * ULID or UUID identifier.
   */
  milestone_id?: string;
  /**
   * RFC 3339 / ISO 8601 timestamp in UTC.
   */
  scheduled_at: string;
  /**
   * Provider response latency — consumed by Provider Success.
   */
  response_seconds?: number;
}

/**
 * payload of appointment.declined (v1)
 */

export interface AppointmentDeclined {
  /**
   * ULID or UUID identifier.
   */
  appointment_id: string;
  /**
   * ULID or UUID identifier.
   */
  user_id: string;
  /**
   * ULID or UUID identifier.
   */
  provider_id: string;
  /**
   * ULID or UUID identifier.
   */
  service_id: string;
  /**
   * ULID or UUID identifier.
   */
  milestone_id?: string;
  /**
   * RFC 3339 / ISO 8601 timestamp in UTC.
   */
  scheduled_at: string;
  reason: "no_capacity" | "out_of_scope" | "schedule_conflict" | "other";
  response_seconds?: number;
}

/**
 * payload of appointment.completed (v1) — fan-out to Transformation, Provider Success, Commercial, Review, ML.
 */

export interface AppointmentCompleted {
  /**
   * ULID or UUID identifier.
   */
  appointment_id: string;
  /**
   * ULID or UUID identifier.
   */
  user_id: string;
  /**
   * ULID or UUID identifier.
   */
  provider_id: string;
  /**
   * ULID or UUID identifier.
   */
  service_id: string;
  /**
   * ULID or UUID identifier.
   */
  milestone_id?: string;
  /**
   * RFC 3339 / ISO 8601 timestamp in UTC.
   */
  scheduled_at: string;
  /**
   * RFC 3339 / ISO 8601 timestamp in UTC.
   */
  completed_at: string;
  duration_minutes?: number;
  amount?: Money;
}
/**
 * Monetary amount. Integer minor units to avoid float error.
 */

export interface Money {
  /**
   * Amount in minor units (e.g. fils for AED).
   */
  amount_minor: number;
  /**
   * ISO 4217 currency code.
   */
  currency: string;
}

/**
 * payload of appointment.no_show (v1)
 */

export interface AppointmentNoShow {
  /**
   * ULID or UUID identifier.
   */
  appointment_id: string;
  /**
   * ULID or UUID identifier.
   */
  user_id: string;
  /**
   * ULID or UUID identifier.
   */
  provider_id: string;
  /**
   * ULID or UUID identifier.
   */
  service_id: string;
  /**
   * ULID or UUID identifier.
   */
  milestone_id?: string;
  /**
   * RFC 3339 / ISO 8601 timestamp in UTC.
   */
  scheduled_at: string;
  party: "customer" | "provider";
}

/**
 * payload of assessment.completed (v1) — Transformation builds the roadmap.
 */

export interface AssessmentCompleted {
  /**
   * ULID or UUID identifier.
   */
  assessment_id: string;
  /**
   * ULID or UUID identifier.
   */
  user_id: string;
  kind: "beauty" | "body" | "wellness" | "longevity" | "intake" | "reassessment";
  recommended_goals?: string[];
  scores?: {
    /**
     * Normalized score in the [0,1] range.
     */
    [k: string]: number;
  };
  /**
   * RFC 3339 / ISO 8601 timestamp in UTC.
   */
  completed_at: string;
}

/**
 * payload of commercial.offer_recommended (v1) — Transformation presents at the right moment.
 */

export interface CommercialOfferRecommended {
  /**
   * ULID or UUID identifier.
   */
  offer_id: string;
  /**
   * ULID or UUID identifier.
   */
  user_id: string;
  offer_type: "upgrade" | "add_on" | "bundle" | "renewal" | "win_back" | "cross_sell";
  /**
   * ULID or UUID identifier.
   */
  segment_id?: string;
  expected_uplift?: Money;
  price?: Money;
  channel?: "app" | "web" | "whatsapp" | "email" | "sms" | "push" | "in_app" | "system";
  /**
   * RFC 3339 / ISO 8601 timestamp in UTC.
   */
  valid_to: string;
}
/**
 * Monetary amount. Integer minor units to avoid float error.
 */

/**
 * payload of commercial.upsell_opportunity (v1) — feeds Transformation NBA.
 */

export interface CommercialUpsellOpportunity {
  /**
   * ULID or UUID identifier.
   */
  user_id: string;
  recommendation: string;
  target_plan_id?: string;
  /**
   * Normalized score in the [0,1] range.
   */
  uplift_score: number;
  best_moment?: "onboarding" | "milestone" | "renewal" | "post_outcome" | "reactivation";
}

/**
 * payload of commercial.segment_updated (v1)
 */

export interface CommercialSegmentUpdated {
  /**
   * ULID or UUID identifier.
   */
  segment_id: string;
  name?: string;
  type: "rfm" | "behavioral" | "value" | "lifecycle" | "luxury_spend";
  size: number;
  /**
   * RFC 3339 / ISO 8601 timestamp in UTC.
   */
  updated_at: string;
}

/**
 * payload of commercial.price_suggestion (v1) — Subscription/Marketing apply.
 */

export interface CommercialPriceSuggestion {
  scope: "plan" | "add_on" | "bundle" | "segment";
  scope_ref?: string;
  suggested_price: Money;
  elasticity?: number;
  /**
   * Normalized score in the [0,1] range.
   */
  confidence?: number;
  /**
   * ULID or UUID identifier.
   */
  experiment_id?: string;
  /**
   * RFC 3339 / ISO 8601 timestamp in UTC.
   */
  generated_at: string;
}
/**
 * Monetary amount. Integer minor units to avoid float error.
 */

/**
 * payload of commercial.forecast_updated (v1)
 */

export interface CommercialForecastUpdated {
  metric: "mrr" | "arr" | "churn_revenue" | "expansion" | "nrr";
  horizon_days: number;
  point_estimate?: Money;
  ci_low?: Money;
  ci_high?: Money;
  model_version?: string;
  /**
   * RFC 3339 / ISO 8601 timestamp in UTC.
   */
  generated_at: string;
}
/**
 * Monetary amount. Integer minor units to avoid float error.
 */

/**
 * payload of commercial.kpi_alert (v1) — surfaced in Commercial Intelligence Center + Monitoring.
 */

export interface CommercialKpiAlert {
  kpi: string;
  severity: "info" | "warning" | "critical";
  value: number;
  target?: number;
  delta_pct?: number;
  /**
   * RFC 3339 / ISO 8601 timestamp in UTC.
   */
  triggered_at: string;
}

/**
 * payload of auth.user_registered (v1)
 */

export interface AuthUserRegistered {
  /**
   * ULID or UUID identifier.
   */
  user_id: string;
  channel?: "app" | "web" | "whatsapp" | "email" | "sms" | "push" | "in_app" | "system";
  /**
   * RFC 3339 / ISO 8601 timestamp in UTC.
   */
  registered_at: string;
}

/**
 * payload of auth.session_revoked (v1)
 */

export interface AuthSessionRevoked {
  /**
   * ULID or UUID identifier.
   */
  user_id: string;
  /**
   * ULID or UUID identifier.
   */
  session_id?: string;
  scope: "single" | "all";
  reason?: "logout" | "logout_all" | "token_reuse" | "admin" | "security";
  /**
   * RFC 3339 / ISO 8601 timestamp in UTC.
   */
  revoked_at: string;
}

/**
 * payload of auth.security_event (v1) — consumed by Monitoring.
 */

export interface AuthSecurityEvent {
  /**
   * ULID or UUID identifier.
   */
  user_id?: string;
  kind: "brute_force" | "impossible_travel" | "new_device" | "token_reuse" | "mfa_failure" | "lockout";
  severity: "info" | "warning" | "critical";
  ip?: string;
  /**
   * RFC 3339 / ISO 8601 timestamp in UTC.
   */
  detected_at: string;
}

export interface JourneyRef {
  /**
   * ULID or UUID identifier.
   */
  journey_id: string;
  /**
   * ULID or UUID identifier.
   */
  user_id: string;
}

/**
 * payload of journey.created (v1)
 */

export interface JourneyCreated {
  /**
   * ULID or UUID identifier.
   */
  journey_id: string;
  /**
   * ULID or UUID identifier.
   */
  user_id: string;
  /**
   * @minItems 1
   */
  primary_goals: [string, ...string[]];
  status: "draft" | "onboarding" | "active";
  /**
   * RFC 3339 / ISO 8601 timestamp in UTC.
   */
  started_at: string;
  /**
   * RFC 3339 / ISO 8601 timestamp in UTC.
   */
  target_completion?: string;
  ltv_projection?: Money;
}
/**
 * Monetary amount. Integer minor units to avoid float error.
 */

/**
 * payload of journey.roadmap_updated (v1)
 */

export interface JourneyRoadmapUpdated {
  /**
   * ULID or UUID identifier.
   */
  journey_id: string;
  /**
   * ULID or UUID identifier.
   */
  user_id: string;
  milestone_count: number;
  reason: "assessment" | "reassessment" | "outcome" | "manual" | "ai_recommendation";
}

/**
 * payload of journey.milestone_completed (v1)
 */

export interface JourneyMilestoneCompleted {
  /**
   * ULID or UUID identifier.
   */
  journey_id: string;
  /**
   * ULID or UUID identifier.
   */
  user_id: string;
  /**
   * ULID or UUID identifier.
   */
  milestone_id: string;
  /**
   * ULID or UUID identifier.
   */
  phase_id?: string;
  /**
   * ULID or UUID identifier.
   */
  provider_id?: string;
  /**
   * ULID or UUID identifier.
   */
  appointment_id?: string;
  /**
   * Normalized score in the [0,1] range.
   */
  outcome_delta?: number;
  /**
   * RFC 3339 / ISO 8601 timestamp in UTC.
   */
  completed_at: string;
}

/**
 * payload of journey.phase_completed (v1)
 */

export interface JourneyPhaseCompleted {
  /**
   * ULID or UUID identifier.
   */
  journey_id: string;
  /**
   * ULID or UUID identifier.
   */
  user_id: string;
  /**
   * ULID or UUID identifier.
   */
  phase_id: string;
  /**
   * RFC 3339 / ISO 8601 timestamp in UTC.
   */
  completed_at: string;
}

/**
 * payload of journey.next_action (v1)
 */

export interface JourneyNextAction {
  /**
   * ULID or UUID identifier.
   */
  journey_id: string;
  /**
   * ULID or UUID identifier.
   */
  user_id: string;
  action_type: "book" | "assess" | "renew" | "upsell" | "adherence" | "follow_up" | "contact";
  priority?: "low" | "medium" | "high" | "urgent";
  /**
   * RFC 3339 / ISO 8601 timestamp in UTC.
   */
  due_at?: string;
}

/**
 * payload of journey.at_risk (v1)
 */

export interface JourneyAtRisk {
  /**
   * ULID or UUID identifier.
   */
  journey_id: string;
  /**
   * ULID or UUID identifier.
   */
  user_id: string;
  risk_reason: "payment_failed" | "low_adherence" | "no_show" | "negative_review" | "inactivity" | "churn_model";
  /**
   * Normalized score in the [0,1] range.
   */
  churn_risk: number;
}

/**
 * payload of journey.reassessment_due (v1)
 */

export interface JourneyReassessmentDue {
  /**
   * ULID or UUID identifier.
   */
  journey_id: string;
  /**
   * ULID or UUID identifier.
   */
  user_id: string;
  /**
   * RFC 3339 / ISO 8601 timestamp in UTC.
   */
  due_at: string;
}

/**
 * payload of journey.completed (v1)
 */

export interface JourneyCompleted {
  /**
   * ULID or UUID identifier.
   */
  journey_id: string;
  /**
   * ULID or UUID identifier.
   */
  user_id: string;
  /**
   * RFC 3339 / ISO 8601 timestamp in UTC.
   */
  completed_at: string;
  /**
   * Normalized score in the [0,1] range.
   */
  outcome_score: number;
  realized_ltv?: Money;
}
/**
 * Monetary amount. Integer minor units to avoid float error.
 */

/**
 * payload of marketing.campaign_sent (v1) — consumed by Commercial (attribution/ROI).
 */

export interface MarketingCampaignSent {
  /**
   * ULID or UUID identifier.
   */
  campaign_id: string;
  channel: "app" | "web" | "whatsapp" | "email" | "sms" | "push" | "in_app" | "system";
  /**
   * ULID or UUID identifier.
   */
  offer_id?: string;
  /**
   * ULID or UUID identifier.
   */
  segment_id?: string;
  audience_size: number;
  /**
   * RFC 3339 / ISO 8601 timestamp in UTC.
   */
  sent_at: string;
}

/**
 * payload of marketing.engagement (v1)
 */

export interface MarketingEngagement {
  /**
   * ULID or UUID identifier.
   */
  campaign_id: string;
  /**
   * ULID or UUID identifier.
   */
  user_id: string;
  action: "delivered" | "opened" | "clicked" | "converted" | "unsubscribed";
  /**
   * RFC 3339 / ISO 8601 timestamp in UTC.
   */
  occurred_at: string;
}

/**
 * payload of medication.intake_missed (v1) — feeds adherence score + journey health.
 */

export interface MedicationIntakeMissed {
  /**
   * ULID or UUID identifier.
   */
  prescription_id: string;
  /**
   * ULID or UUID identifier.
   */
  user_id: string;
  /**
   * ULID or UUID identifier.
   */
  schedule_entry_id?: string;
  /**
   * RFC 3339 / ISO 8601 timestamp in UTC.
   */
  scheduled_for: string;
  /**
   * RFC 3339 / ISO 8601 timestamp in UTC.
   */
  missed_at: string;
}

/**
 * payload of medication.refill_due (v1)
 */

export interface MedicationRefillDue {
  /**
   * ULID or UUID identifier.
   */
  prescription_id: string;
  /**
   * ULID or UUID identifier.
   */
  user_id: string;
  /**
   * RFC 3339 / ISO 8601 timestamp in UTC.
   */
  due_at: string;
}

/**
 * payload of ml.model_promoted (v1) — a new model version passed A/B + guardrails and is now serving.
 */

export interface MlModelPromoted {
  model:
    | "provider_ranker"
    | "provider_churn"
    | "customer_churn"
    | "nba_upsell"
    | "recommendation"
    | "adherence_risk"
    | "forecasting"
    | "segmentation"
    | "price_elasticity"
    | "ltv";
  version: string;
  metric?: string;
  metric_value?: number;
  /**
   * RFC 3339 / ISO 8601 timestamp in UTC.
   */
  promoted_at: string;
}

/**
 * payload of ml.prediction_ready (v1) — batch prediction set available for a consuming engine.
 */

export interface MlPredictionReady {
  model: string;
  /**
   * ULID or UUID identifier.
   */
  batch_id: string;
  count: number;
  /**
   * RFC 3339 / ISO 8601 timestamp in UTC.
   */
  ready_at: string;
}

/**
 * payload of payment.succeeded (v1)
 */

export interface PaymentSucceeded {
  /**
   * ULID or UUID identifier.
   */
  payment_id: string;
  /**
   * ULID or UUID identifier.
   */
  user_id: string;
  /**
   * ULID or UUID identifier.
   */
  subscription_id?: string;
  /**
   * ULID or UUID identifier.
   */
  invoice_id?: string;
  amount: Money;
  gateway: "tap_payments" | "stripe";
  purpose?: "subscription" | "marketplace_order" | "add_on" | "top_up";
  /**
   * RFC 3339 / ISO 8601 timestamp in UTC.
   */
  captured_at: string;
}
/**
 * Monetary amount. Integer minor units to avoid float error.
 */

/**
 * payload of payment.failed (v1) — triggers dunning + journey.at_risk.
 */

export interface PaymentFailed {
  /**
   * ULID or UUID identifier.
   */
  payment_id: string;
  /**
   * ULID or UUID identifier.
   */
  user_id: string;
  /**
   * ULID or UUID identifier.
   */
  subscription_id?: string;
  amount: Money;
  gateway: "tap_payments" | "stripe";
  failure_code: string;
  dunning_attempt?: number;
  /**
   * RFC 3339 / ISO 8601 timestamp in UTC.
   */
  failed_at: string;
}
/**
 * Monetary amount. Integer minor units to avoid float error.
 */

/**
 * payload of commission.computed (v1) — consumed by Provider Success + Commercial.
 */

export interface CommissionComputed {
  /**
   * ULID or UUID identifier.
   */
  commission_id: string;
  /**
   * ULID or UUID identifier.
   */
  provider_id: string;
  /**
   * ULID or UUID identifier.
   */
  payment_id?: string;
  /**
   * ULID or UUID identifier.
   */
  appointment_id?: string;
  gross?: Money;
  amount: Money;
  /**
   * Normalized score in the [0,1] range.
   */
  rate?: number;
  /**
   * RFC 3339 / ISO 8601 timestamp in UTC.
   */
  computed_at: string;
}
/**
 * Monetary amount. Integer minor units to avoid float error.
 */

/**
 * payload of profile.updated (v1)
 */

export interface ProfileUpdated {
  /**
   * ULID or UUID identifier.
   */
  user_id: string;
  /**
   * @minItems 1
   */
  updated_fields: [string, ...string[]];
  lifecycle_stage?: "lead" | "onboarding" | "active" | "at_risk" | "churned" | "reactivated";
  vip_tier?: "none" | "silver" | "gold" | "black";
  /**
   * RFC 3339 / ISO 8601 timestamp in UTC.
   */
  updated_at: string;
}

/**
 * payload of provider.score_updated (v1) — consumed by Matching for ranking.
 */

export interface ProviderScoreUpdated {
  /**
   * ULID or UUID identifier.
   */
  provider_id: string;
  /**
   * Normalized score in the [0,1] range.
   */
  success_score: number;
  /**
   * Normalized score in the [0,1] range.
   */
  quality_score?: number;
  tier: "bronze" | "silver" | "gold" | "platinum";
  /**
   * RFC 3339 / ISO 8601 timestamp in UTC.
   */
  computed_at: string;
}

/**
 * payload of provider.capacity_updated (v1) — consumed by Matching (capacity-aware).
 */

export interface ProviderCapacityUpdated {
  /**
   * ULID or UUID identifier.
   */
  provider_id: string;
  /**
   * Normalized score in the [0,1] range.
   */
  utilization: number;
  available: boolean;
  open_slots_next_7d?: number;
  /**
   * RFC 3339 / ISO 8601 timestamp in UTC.
   */
  updated_at: string;
}

/**
 * payload of provider.at_risk (v1)
 */

export interface ProviderAtRisk {
  /**
   * ULID or UUID identifier.
   */
  provider_id: string;
  risk_reason: "low_acceptance" | "slow_response" | "low_satisfaction" | "sla_breach" | "complaints" | "inactivity";
  /**
   * Normalized score in the [0,1] range.
   */
  success_score?: number;
  /**
   * RFC 3339 / ISO 8601 timestamp in UTC.
   */
  detected_at: string;
}

/**
 * payload of provider.lifecycle_changed (v1)
 */

export interface ProviderLifecycleChanged {
  /**
   * ULID or UUID identifier.
   */
  provider_id: string;
  from_stage: "onboarding" | "ramping" | "active" | "top_performer" | "underperforming" | "probation" | "offboarded";
  to_stage: "onboarding" | "ramping" | "active" | "top_performer" | "underperforming" | "probation" | "offboarded";
  /**
   * RFC 3339 / ISO 8601 timestamp in UTC.
   */
  changed_at: string;
}

/**
 * payload of provider.incentive_awarded (v1)
 */

export interface ProviderIncentiveAwarded {
  /**
   * ULID or UUID identifier.
   */
  provider_id: string;
  /**
   * ULID or UUID identifier.
   */
  program_id: string;
  award: Money;
  reason?: string;
  /**
   * RFC 3339 / ISO 8601 timestamp in UTC.
   */
  awarded_at: string;
}
/**
 * Monetary amount. Integer minor units to avoid float error.
 */

/**
 * payload of review.created (v1) — triggers Quality Score recompute; consumed by Provider Success.
 */

export interface ReviewCreated {
  /**
   * ULID or UUID identifier.
   */
  review_id: string;
  /**
   * ULID or UUID identifier.
   */
  provider_id: string;
  /**
   * ULID or UUID identifier.
   */
  user_id: string;
  /**
   * ULID or UUID identifier.
   */
  appointment_id?: string;
  rating: number;
  /**
   * Per-dimension ratings (e.g. punctuality, professionalism).
   */
  dimensions?: {
    [k: string]: number;
  };
  verified?: boolean;
  /**
   * RFC 3339 / ISO 8601 timestamp in UTC.
   */
  created_at: string;
}

/**
 * payload of review.moderated (v1)
 */

export interface ReviewModerated {
  /**
   * ULID or UUID identifier.
   */
  review_id: string;
  decision: "approved" | "rejected" | "flagged";
  /**
   * RFC 3339 / ISO 8601 timestamp in UTC.
   */
  moderated_at: string;
}

/**
 * payload of subscription.created (v1)
 */

export interface SubscriptionCreated {
  /**
   * ULID or UUID identifier.
   */
  subscription_id: string;
  /**
   * ULID or UUID identifier.
   */
  user_id: string;
  plan_id: string;
  price: Money;
  billing_cycle: "monthly" | "quarterly" | "annual";
  /**
   * RFC 3339 / ISO 8601 timestamp in UTC.
   */
  started_at: string;
}
/**
 * Monetary amount. Integer minor units to avoid float error.
 */

/**
 * payload of subscription.renewed (v1)
 */

export interface SubscriptionRenewed {
  /**
   * ULID or UUID identifier.
   */
  subscription_id: string;
  /**
   * ULID or UUID identifier.
   */
  user_id: string;
  plan_id: string;
  price: Money;
  /**
   * RFC 3339 / ISO 8601 timestamp in UTC.
   */
  renewed_at: string;
  /**
   * RFC 3339 / ISO 8601 timestamp in UTC.
   */
  period_end: string;
}
/**
 * Monetary amount. Integer minor units to avoid float error.
 */

/**
 * payload of subscription.upgraded (v1)
 */

export interface SubscriptionUpgraded {
  /**
   * ULID or UUID identifier.
   */
  subscription_id: string;
  /**
   * ULID or UUID identifier.
   */
  user_id: string;
  from_plan_id: string;
  to_plan_id: string;
  mrr_delta?: Money;
  /**
   * ULID or UUID identifier.
   */
  triggered_by_offer_id?: string;
  /**
   * RFC 3339 / ISO 8601 timestamp in UTC.
   */
  changed_at: string;
}
/**
 * Monetary amount. Integer minor units to avoid float error.
 */

/**
 * payload of subscription.downgraded (v1)
 */

export interface SubscriptionDowngraded {
  /**
   * ULID or UUID identifier.
   */
  subscription_id: string;
  /**
   * ULID or UUID identifier.
   */
  user_id: string;
  from_plan_id: string;
  to_plan_id: string;
  mrr_delta?: Money;
  /**
   * RFC 3339 / ISO 8601 timestamp in UTC.
   */
  changed_at: string;
}
/**
 * Monetary amount. Integer minor units to avoid float error.
 */

/**
 * payload of subscription.churned (v1)
 */

export interface SubscriptionChurned {
  /**
   * ULID or UUID identifier.
   */
  subscription_id: string;
  /**
   * ULID or UUID identifier.
   */
  user_id: string;
  plan_id: string;
  reason: "voluntary" | "payment_failure" | "downgrade_to_none" | "expired";
  lost_mrr?: Money;
  /**
   * RFC 3339 / ISO 8601 timestamp in UTC.
   */
  churned_at: string;
}
/**
 * Monetary amount. Integer minor units to avoid float error.
 */
